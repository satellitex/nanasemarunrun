<%= form_for(@passport) do |f| %>
  <% if @passport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passport.errors.count, "error") %> prohibited this passport from being saved:</h2>

      <ul>
      <% @passport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :passport %><br>
    <%= f.check_box :passport %>
  </div>
  <div class="field">
    <%= f.label :lastupdated %><br>
    <%= f.text_field :lastupdated %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
